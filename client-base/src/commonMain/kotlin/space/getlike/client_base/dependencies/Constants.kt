package space.getlike.client_base.dependencies

object Constants {

    object Settings {
        const val NAME = "settings"
    }

    object Database {
        const val NAME = "getlike.db"
    }

    object Logger {
        const val CUSTOM_KEY_PLATFORM = "platform"
    }
}